CRITICAL: Before starting any new task or writing code, you must read the files in `docs/ai/` to understand the project context and architecture. At the end of every completed task, you must update `docs/ai/progress.md` and `docs/ai/active_context.md` to reflect the new state of the project.
⚠️ CONTEXT WINDOW GUARDRAIL (1/3 RULE): Monitor your token/context usage closely. If you estimate you have consumed roughly 1/3 of your maximum context window, you must STOP immediately. Do not start the next sub-task. Before exiting: 1) Update `docs/ai/progress.md` with achievements. 2) Update `docs/ai/active_context.md` with the exact stop point and next steps. 3) End your response to let the user reset the session.
